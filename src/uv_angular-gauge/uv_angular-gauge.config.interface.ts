export default interface UVAngularGaugeConfig {
  chartMax: number;
  chartMin: number;
  hand ?: any;
  handAxis ?: any;
  mainAxis ?: any;
  score: number;
  title: string;
  upperCaseGrades ?: boolean;
};
