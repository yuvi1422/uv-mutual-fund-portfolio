export default interface UVAngularGaugeConfig {
  chartMax: number;
  chartMin: number;
  hand ?: any;
  handAxis ?: any;
  mainAxis ?: any;
  score: number;
  showScore ?: boolean;
  title: string;
  upperCaseGrades ?: boolean;
};
